package com.example.groove.ui.theme

import androidx.compose.ui.graphics.Color

// Groove brand tokens
val GrooveDark       = Color(0xFF071400)
val GrooveSageGreen  = Color(0xFF5CA253)
val GrooveWhiteSmoke = Color(0xFFF7F6F3)

// Derived surface / dim variants
val GrooveDarkSurface  = Color(0xFF0D2209)   // slightly lighter dark for gradients / cards
val GrooveDimGreen     = Color(0xFF1D3B1A)   // inactive dot / muted surface
val GrooveTextSecondary = Color(0xFFA8C9A5)  // muted sage for secondary text
