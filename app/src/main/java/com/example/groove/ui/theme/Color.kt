package com.example.groove.ui.theme

import androidx.compose.ui.graphics.Color

// Groove brand tokens
val GrooveBlack      = Color(0xFF000000)   // night (dark) theme background
val GrooveDark       = Color(0xFF071400)   // dark text on light surfaces / onPrimary in dark theme
val GrooveSageGreen  = Color(0xFF5CA253)   // primary accent — buttons, active icons
val GrooveWhiteSmoke = Color(0xFFF7F6F3)   // day (light) theme background + text on dark surfaces

// Derived surface / dim variants
val GrooveDarkSurface  = Color(0xFF0D2209)   // dark theme cards / gradient surfaces
val GrooveDimGreen     = Color(0xFF1D3B1A)   // inactive dot / muted surface (secondary in both themes)
val GrooveTextSecondary = Color(0xFFA8C9A5)  // muted sage for secondary text
