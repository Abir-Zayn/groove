package com.example.groove.controller

import android.net.Uri

object FileState {
    var pendingUri: Uri? = null
}

class HomeController {
    // Manages home screen business logic — file upload state, navigation triggers
}
